import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
const names:any={scenepacks:'Scenepacks','after-effects':'After Effects',presets:'Presets',luts:'LUTs','sound-effects':'Sound Effects',vfx:'VFX',overlays:'Overlays',tutorials:'Tutorials','project-files':'Project Files',fonts:'Fonts'};
export default async function Category({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const name=names[slug]??'Resources';return <main><header className="site-header"><Link href="/" className="brand">CYRIQ<span>VFX</span></Link><nav><Link href="/resources">Resources</Link><Link href="/categories">Categories</Link></nav><Link href="/admin/login" className="creator-link">Creator Studio <ArrowUpRight size={15}/></Link></header><section className="container listing-head"><span className="kicker">CATEGORY</span><h1>{name}.</h1><p>Browse everything currently published in {name.toLowerCase()}.</p><Link className="text-link" href={`/resources?category=${slug}`}>Open filtered library <ArrowUpRight size={15}/></Link></section></main>}
