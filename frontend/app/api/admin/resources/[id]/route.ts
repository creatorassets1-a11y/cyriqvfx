import { NextResponse } from 'next/server';
import { requireAdmin } from '@/lib/auth';
import { createAdminClient } from '@/lib/supabase/admin';
export async function PATCH(request:Request,context:{params:Promise<{id:string}>}){const admin=await requireAdmin();if(!admin)return NextResponse.json({error:'Unauthorized'},{status:401});const {id}=await context.params;const body=await request.json();const allowed={published:body.published,featured:body.featured,title:body.title,description:body.description,tags:body.tags,software:body.software,version:body.version,license:body.license,thumbnail_url:body.thumbnailUrl,preview_url:body.previewUrl};const clean=Object.fromEntries(Object.entries(allowed).filter(([,v])=>v!==undefined));const supa=createAdminClient();const {data,error}=await supa.from('resources').update(clean).eq('id',id).select('id,slug,published,featured').single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({resource:data})}
export async function DELETE(_:Request,context:{params:Promise<{id:string}>}){const admin=await requireAdmin();if(!admin)return NextResponse.json({error:'Unauthorized'},{status:401});const {id}=await context.params;const supa=createAdminClient();const {error}=await supa.from('resources').delete().eq('id',id);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true})}
