import Link from 'next/link';
import { ArrowLeft, Download, ExternalLink } from 'lucide-react';
export default async function ResourcePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;return <main><header className="site-header"><Link href="/" className="brand">CYRIQ<span>VFX</span></Link><nav><Link href="/resources">Resources</Link><Link href="/categories">Categories</Link></nav><Link href="/admin/login" className="creator-link">Creator Studio <ExternalLink size={14}/></Link></header><section className="container resource-detail"><Link href="/resources" className="back"><ArrowLeft size={14}/> Back to resources</Link><div className="detail-art"><span>RESOURCE / {slug}</span></div><div className="detail-copy"><span className="kicker">FREE DOWNLOAD</span><h1>{slug.replaceAll('-',' ')}</h1><p>This resource page is ready for the published asset, metadata, usage information and download action. Once published from Creator Studio, visitors receive a temporary secure download link from storage.</p><div className="detail-actions"><Link className="primary-btn" href={`/api/resources/${encodeURIComponent(slug)}/download`}><Download size={17}/> Download</Link><button className="secondary-btn" onClick={()=>navigator.clipboard?.writeText(location.href)}><ExternalLink size={15}/> Copy link</button></div></div></section></main>}
