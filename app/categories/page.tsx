import Link from 'next/link';
import { ArrowRight, Film, Wand2, Palette, Volume2, Layers, BookOpen, Sparkles, Package } from 'lucide-react';
const cats=[['Scenepacks',Film],['Presets',Wand2],['LUTs',Palette],['Sound Effects',Volume2],['VFX',Sparkles],['Overlays',Layers],['After Effects',Package],['Tutorials',BookOpen]];
export default function Categories(){return <main className="shell"><header className="nav"><Link href="/" className="brand"><span className="brand-mark">C</span>CYRIQVFX</Link><Link href="/resources" className="text-link">Browse resources <ArrowRight size={15}/></Link></header><section className="section" style={{paddingTop:80}}><span className="eyebrow small">BROWSE BY TYPE</span><h1 style={{fontSize:'clamp(46px,7vw,72px)'}}>Pick a category.<br/><em>Start creating.</em></h1><div className="category-grid" style={{marginTop:45}}>{cats.map(([name,Icon],i)=><Link className="category" href={'/categories/'+String(name).toLowerCase().replaceAll(' ','-')} key={String(name)}><span>{String(i+1).padStart(2,'0')}</span><b style={{display:'flex',gap:12,alignItems:'center'}}><Icon size={19}/>{String(name)}</b><ArrowRight size={16}/></Link>)}</div></section></main>}
